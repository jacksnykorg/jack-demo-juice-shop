name: Snyk Security Demo

on:
  push:
    branches: [ CICD-demo ]

 
jobs:
  security-scan:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '14'

      - name: Install Dependencies
        run: npm ci

      # ================================
      # Dependency Scan (snyk test) 
      # ================================
      - name: Snyk vulnerability scan
        uses: snyk/actions/node@master 
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: test
          args: '--all-projects --ignore-policy --severity-threshold=high --sarif-file-output=frontend-deps.sarif'

      - name: Upload frontend security report
        uses: github/codeql-action/upload-sarif@v3
        # FIXED: Added always()
        if: always() && hashFiles('frontend-deps.sarif') != ''
        with:
          sarif_file: frontend-deps.sarif
          category: frontend-deps

      # ================================
      # Code Scan (snyk code test)
      # ================================
      - name: Snyk code quality scan
        uses: snyk/actions/node@master
        continue-on-error: true # Ensures upload step always runs
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: code test
          args: '--sarif-file-output=code-scan.sarif'

      - name: Replace level note/warning/error for SAST findings
        # Add an 'if' to only run if the file exists
        if: hashFiles('code-scan.sarif') != ''
        run: | 
          jq '
            walk(
              if type == "object" and has("id") and has("defaultConfiguration") and .defaultConfiguration.level then
                # Create or update properties bag with security-severity
                if has("properties") then
                  .properties += {"security-severity": (
                    if .defaultConfiguration.level == "note" then "2"
                    elif .defaultConfiguration.level == "warning" then "5" 
                    elif .defaultConfiguration.level == "error" then "8"
                    else "0"
                    end
                  )}
                else
                  . + {"properties": {"security-severity": (
                    if .defaultConfiguration.level == "note" then "2"
                    elif .defaultConfiguration.level == "warning" then "5" 
                    elif .defaultConfiguration.level == "error" then "8"
                    else "0"
                    end
                  )}}
                end
              else .
              end
            )
          ' code-scan.sarif > code-scan_updated.sarif && mv code-scan_updated.sarif code-scan.sarif

      - name: Upload code quality report
        uses: github/codeql-action/upload-sarif@v3
        # This condition is correct: always run, but only upload if file exists
        if: always() && hashFiles('code-scan.sarif') != ''
        with:
          sarif_file: code-scan.sarif
          category: code-scan


      # ================================
      # Setup Docker
      # ================================
      - name: Set up Docker Build
        uses: docker/login-action@v3
        with:
          registry: docker.io 
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      # ================================
      # Container Scan (snyk Container test)
      # ================================

      - name: Snyk Container  scan
        uses: snyk/actions/node@master
        continue-on-error: true # Ensures upload step always runs
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: container test docker.io/${{ secrets.DOCKERHUB_USERNAME }}/${{ vars.IMAGE_TAG }}:latest
          args: '--sarif-file-output=container-scan.sarif --severity-threshold=low'
      - name: Patch Snyk Container SARIF
        run: |
          # Install jq if not present (standard on ubuntu-latest)
          # This script maps text severities to the numbers GitHub expects
          jq '
            (.runs[].tool.driver.rules[] | select(.properties["security-severity"] == "null")) 
            |= (.properties["security-severity"] = "0.0") 
            | 
            (.runs[].tool.driver.rules[]) |= (
              .properties["security-severity"] |= (
                if . == "null" or . == null then
                  if .properties.severity == "Critical" then "9.5"
                  elif .properties.severity == "High" then "8.0"
                  elif .properties.severity == "Medium" then "5.0"
                  elif .properties.severity == "Low" then "2.0"
                  else "0.0" end
                else . end
              )
            )
          ' container-scan.sarif > container-scan-patched.sarif

      - name: Upload container scan report
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          # Make sure to upload the PATCHED file, not the original
          sarif_file: container-scan-patched.sarif
          category: container-scan

